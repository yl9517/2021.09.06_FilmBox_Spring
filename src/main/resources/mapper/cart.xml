<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.film.mapper.CartMapper">

<insert id="insertCart" parameterType="com.film.dto.ProductDTO">
        insert into filmbox_cart (
        						 member_id
        						, product_no
        						, cart_count
        						, cart_price
        						, cart_date)
        	values( #{member_id}, #{product_no}, #{cart_count},
        			#{cart_price}, now() )          
</insert>

</mapper>